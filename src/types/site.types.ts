export interface SiteConfig {
  name: string; legalName: string; slogan: string; phone: string; whatsapp: string;
  email: string; address: string; instagramUrl?: string; facebookUrl?: string; tiktokUrl?: string;
}
